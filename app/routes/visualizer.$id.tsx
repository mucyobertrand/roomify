import { useState } from "react";

export default function Visualizer() {
    const [id, setId] = useState<string>("");
    
    return (
        <div>
            <h1>Visualizer</h1>
        </div>
    );
}